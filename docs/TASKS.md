# WARMAPS Tasks & Ideas

## 🔴 Priority: Fix
- [x] ~~**Empty State Handling**~~ — ✅ DONE. `perf.tsx` now tracks consecutive failed pings to the `/api/ping` endpoint every 5 seconds. If the backend drops (2 fails), it gracefully captures the screen with a frosted glass full-screen `CONNECTION LOST` boundary overlay, which dissipates once the connection restores.
- [x] ~~**Canvas event passthrough broken**~~ — ✅ DONE. Map zoom triggered canvas zoom, feed scroll didn't work. Root cause: GalaxyDraw engine's `consumesWheel`/`consumesMouse` only looked for `.gd-card` elements, but WARMAPS containers are `.wm-container`. Fix: (1) engine now also checks `[data-card-type]` attribute, (2) warmaps-canvas.ts stamps `data-card-type` on containers. Published `galaxydraw@0.2.0` and deployed.
- [x] ~~**SEO references wrong domain**~~ — ✅ DONE. All OG/Twitter/canonical/sitemap/robots.txt references changed from `warmaps.live` → `warmaps.xyz`.
- [x] ~~**Telegram verification code**~~ — ✅ DONE. AUTH_KEY_DUPLICATED resolved. Reconnect API added at `POST /api/telegram/connect` with `{retry:true}`. Added click handlers on the Telegram status bar to open prompts for verification code and 2FA password, posting to `/api/telegram/verify` and `/api/telegram/password`.

## 🟡 Priority: Improve
- [x] ~~**Widget Resize Polish**~~ — ✅ DONE. Resizing a widget previously required hovering an invisible bounding box. Increased hit area and set `.wm-resize-handle` to uniformly render at 0.5 opacity with a bright 0.8 green chevron that scales up identically alongside full opacity on hover.
- [x] ~~**MapLibre Layers Control**~~ — ✅ DONE. Extracted the dropdown layer config from the global map widget into its own dedicated `layer-control` widget block. Added a new rendering loop in `feeds.tsx` that spawns dual-bound HTML5 range sliders, allowing discrete 0-100 opacity control directly against the underlying `mapInstances` paint properties (heatmap, symbol, and circle opacities) while maintaining live updates without blocking the UI.
- [x] ~~**Consolidate domain choice**~~ — ✅ DONE. `warmaps.xyz` is the established canonical domain. Confirmed no remaining references to `.org` or `.live` in the codebase.
- [x] ~~**Heap exceeding allocation**~~ — ✅ DONE. Added `--smol` flag to `package.json` scripts (`npm start` and `npm run dev`) to enforce a lower memory heap limit.
- [x] ~~**TASKS.md was 140 lines**~~ — ✅ DONE. Legacy tasks archived.
- [x] ~~**Character Counter for Geo-Pins**~~ — ✅ DONE. Replaced native `prompt()` with a 500-char limited glassmorphism modal with live counting and validation.

## 🟡 Priority: Improve
- [ ] **Geo-Pin Thread Replies Optimization** — Investigate moving thread replies from localStorage to a more robust indexed storage or lazy-loading them on demand.
- [ ] **Mobile Pinch-to-Zoom Sensitivity** — Fine-tune pinch zooming within the Canvas container for smoother map interactions on touch devices.

## 🟢 Priority: Features
- [ ] **MapLibre Engine Upgrade** — Evaluate and upgrade to the latest MapLibre GL version to leverage any new rendering performance optimizations.

## 🟢 Priority: Features
- [x] ~~**On-chain Geo-Pin Chat**~~ — ✅ DONE. Right-click map → "Post Geo-Pin ⛓️" prompts for a message, signs a Solana Memo transaction via Phantom, records the pin on the server, and renders it as a 💬 marker with hover tooltip showing sender (truncated pubkey), message, timestamp, and solscan tx link. Server API at `/api/geo-pins` provides GET (list) and POST (record). Uses compact `GEOPIN|lat|lng|msg|ts` memo format. Cached with 30s TTL.
- [x] ~~**Map Context Menu Shortcuts**~~ — ✅ DONE. Right-click on any MapLibre map opens a glassmorphism context menu with: Copy Coordinates (clipboard), Drop Pin (animated marker, click to remove), Zoom Here (flyTo), and What's Here? (reverse geocoding via Nominatim). Coordinates shown at bottom. Auto-attaches to all map instances including lazy-loaded ones.
- [x] ~~**Widget Z-Index Stacking Logic**~~ — ✅ DONE. Developed a `bringToFront` Z-Index manager synced automatically with dragging and layout persistence. Any mousedown or touchstart events inside a `.wm-container` automatically push the active widget to the front. Z-indexes track a continuous monotonic counter, and their relative hierarchies serialize down into the `warmaps:layout` local storage buffer and gracefully restore, preventing overlap starvation on dense dense telemetry dashboards.
- [x] ~~**Lottery Rewards System**~~ — ✅ DONE. Created `app/rewards/page.tsx` as a standalone Melina route for users to claim SOL rewards based on accumulated Luck. Added a promotional banner to the main WARMAPS canvas that appears upon Phantom wallet connection.
- [x] ~~**Offline-first resilience**~~ — ✅ DONE. `sw.js` endpoint was added, and the service worker is now properly registered in the client (`app/page.client.tsx`). This allows the app to load from the cache when offline.
- [x] ~~**Dashboard screenshots for README**~~ — ✅ DONE. Captured an actual live screenshot of `https://warmaps.xyz` using a headless Puppeteer script running within the `bun` runtime. The screenshot is now saved as `banner.png` in the repository root for the README.
- [x] ~~**Mobile Touch Optimization**~~ — ✅ DONE. Added `viewport-fit=cover` and `user-scalable=no` to the viewport meta tag, along with `touch-action: none` and `overscroll-behavior: none` to the CSS body. This prevents native double-tap zoom and elastic scroll-bouncing, allowing the canvas engine to perfectly capture native pinches and pans.
- [x] ~~**Data Export**~~ — ✅ DONE. Added "Export Layout JSON" (📥) and "Import Layout JSON" (📤) buttons to the top right toolbar. This allows users to save and load their exact multi-widget canvas setup locally as a `.json` file.

## 🔮 Ideas / Backlog
- [x] ~~**Geo-Pin SQLite Persistence**~~ — ✅ DONE. Added `geo_pins` table to `starwar_users.db` via sqlite-zod-orm. Pins now persist across server restarts. Indexed by `sender` and `signature` for fast lookups.
- [x] ~~**Geo-Pin Heatmap Layer**~~ — ✅ DONE. `addGeoPinHeatmap()` creates a MapLibre heatmap layer (`geo-pins-heatmap`) with purple→yellow color ramp, zoom-responsive radius/intensity/opacity. Real-time updates: `updateHeatmapWithNewPin()` pushes new WS pins into the shared `allLoadedPins` array and updates all map GeoJSON sources. Added to Layer Control widget as "💬 Geo-Pin Density" slider.
- [x] ~~**Geo-Pin Clustering at Low Zoom**~~ — ✅ DONE. Replaced DOM-based `renderGeoPin` with MapLibre native clustered GeoJSON source (`geo-pins-markers`, `cluster: true`, `clusterRadius: 50`, `clusterMaxZoom: 14`). Three layers: purple circle badges with pin count, white count label, and 💬 symbol for unclustered pins. Click cluster → `easeTo` expansion zoom. Hover pin → `Popup` with sender/message/tx link. Click pin → opens Solscan. WebSocket and `postGeoPin` now update the source data instead of appending DOM elements. Dramatically reduces DOM overhead with many pins.
- [x] ~~**Geo-Pin Timeline Filter**~~ — ✅ DONE. API: `GET /api/geo-pins?since=1h|24h|7d|{ms}` — presets convert to epoch thresholds, passed to `getRecentGeoPins(limit, since)` which adds a `WHERE timestamp >= ?` clause. UI: glassmorphism pill bar (`wm-geopin-filter`) at bottom-center of each map with 4 preset buttons (All, 1h, 24h, 7d). Active button highlighted in brand purple. Click re-fetches from API and updates both cluster + heatmap sources instantly.
- [x] ~~**Geo-Pin User Profiles**~~ — ✅ DONE. Server: `getGeoPinsBySender(sender)` and `getGeoPinSenderStats(sender)` queries. API: `GET /api/geo-pins?sender={pubkey}` returns pins + stats (pinCount, firstPin, lastPin). Client: clicking sender address in pin popup opens a glassmorphism profile overlay showing wallet address (copy + Solscan link), stat cards (total pins, active period), latest 5 pins, and action buttons: "🔍 Show Only" (filters cluster/heatmap to sender's pins) and "Show All" (restores). Dismissable with ×, Escape, or clicking outside.
- [x] ~~**Real-time Pin WebSocket**~~ — ✅ DONE. `/ws/geo-pins` channel broadcasts new pins to all connected clients via Bun's pub/sub. Client auto-connects with 5s reconnect. New pins from any user appear instantly on all attached maps without polling.
- [x] ~~**Coordinate Ruler Tool**~~ — ✅ DONE. Right-click → "Measure Distance 📏" sets point A (purple dot), second right-click→Measure sets point B, draws a dashed SVG line between them with a midpoint label showing distance in km/mi (Haversine formula). Dots and line track map pan/zoom. Click label to dismiss.
- [x] ~~**Geo-Pin Reactions**~~ — ✅ DONE. Server: `geo_pin_reactions` SQLite table with (pinSignature, reactor, emoji) primary key. Functions: `savePinReaction()`, `getPinReactions()` (grouped counts), `getReactionsBySignature()`. API: `GET /api/geo-pins/reactions?signature=...` returns counts, `POST` adds reaction (validates against allowed emojis: 👍🔥💀❤️🚀). Client: popup now shows 5 emoji reaction buttons. Clicking posts via Phantom wallet pubkey as reactor. Existing reaction counts load on popup open and display as purple badges. Types: `GeoPinReaction`, `ALLOWED_REACTIONS` exported.
- [x] ~~**Map Annotation Drawing**~~ — ✅ DONE. Right-click → "✏️ Draw Annotation" enters freehand drawing mode. Glassmorphism toolbar at top-center with color picker, stroke width slider (1-10), Clear, Save, Done buttons. Drawing uses SVG overlay with crosshair cursor, map drag disabled. Polylines tracked as lnglat coordinates via `map.unproject()`. Save POSTs to `/api/annotations` which persists to `map_annotations` SQLite table. Saved annotations render as MapLibre GeoJSON line layers with data-driven color/width paint properties. Done exits drawing mode and re-enables map interaction.
- [x] ~~**Geo-Pin Media Attachments**~~ — ✅ DONE. Server: `geo_pin_media` SQLite table storing base64 image data (max 2MB) with MIME type per pin signature. Functions: `savePinMedia()`, `getPinMedia()`. API: `GET /api/geo-pins/media?signature=...` returns image data, `POST` uploads (validates data URL format, enforces 2MB limit, auto-extracts MIME type). Client: pin popup now shows a 📎 Attach Image button that opens a file picker, reads as base64, and POSTs. Existing media loads as a clickable thumbnail (max 120px tall) with purple border. Click thumbnail opens full-size in new tab. Types: `GeoPinMedia` exported.
- [x] ~~**Geo-Pin Thread Replies**~~ — ✅ DONE. Server: `geo_pin_replies` SQLite table with auto-increment id. Functions: `saveReply()`, `getReplies()` (sorted ASC). API: `GET /api/geo-pins/replies?signature=...` returns replies, `POST` adds reply (max 500 chars, validates signature+message). Client: pin popup shows scrollable reply list (sender abbreviation, message, relative time), "💬 Reply" toggle button reveals inline text input + Send button. Uses Phantom wallet pubkey as sender. Types: `GeoPinReply` exported.
- [x] ~~**Geo-Pin Heatmap Toggle**~~ — ✅ ALREADY DONE. The `geo-pins-heatmap` layer is already registered in the Layer Control widget (`feeds.tsx` line 193) as "💬 Geo-Pin Density" with an opacity slider via `setPaintProperty('heatmap-opacity')`. Users can independently show/hide and adjust the heatmap layer opacity.
- [x] ~~**Map Snapshot Export**~~ — ✅ DONE. Right-click → "📸 Capture Map" grabs the MapLibre WebGL canvas via `map.once('render')` + `triggerRepaint()`, composites SVG overlays (ruler lines, annotations) via `XMLSerializer` + `drawImage`, stamps a purple watermark with timestamp, and triggers a download as `warmaps-{timestamp}.png`. Error handling shows toast on failure.
- [x] ~~**Annotation Collaboration**~~ — ✅ DONE. Extended sync WebSocket with `sync:annotation` message type. `broadcastAnnotation()` sends annotation data (coordinates, color, width) to all connected peers. `onRemoteAnnotation()` registers listener callbacks. Handler in `handleSyncMessage` fires all listeners when a peer broadcasts. Drawing Save button now broadcasts each annotation after persisting to API. Module-level listener in `map-context-menu.ts` renders remote annotations as MapLibre GeoJSON line layers on all local map instances.
- [x] ~~**Annotation Eraser Tool**~~ — ✅ DONE. Added 🧹 Eraser toggle button to drawing toolbar (orange when inactive, purple when active). In eraser mode, cursor changes to pointer. Clicking near a polyline stroke (within 15px threshold via `Math.hypot` distance to all points) removes it from both the SVG overlay and the `drawnAnnotations` array. Toggle back to draw mode by clicking the button again.
- [x] ~~**Geo-Pin Categories/Tags**~~ — ✅ DONE. 6 categories: intel(🔍 blue), threat(⚠️ red), friendly(🟢 green), logistics(📦 yellow), observation(👁️ purple), general(💬 gray). `PIN_CATEGORIES` constant + `PinCategory` type exported. `geo_pin_categories` SQLite table (signature PK). API: `GET/POST /api/geo-pins/categories`. Pin popup shows colored badge + dropdown selector. Category loads on popup open, saves on change. Badge updates color/emoji dynamically.
- [x] ~~**Map Measurement Area Tool**~~ — ✅ DONE. `📐 Measure Area` context menu item. Multi-vertex polygon drawing via right-click. Purple dashed SVG polygon with semi-transparent fill. Escape key closes polygon and calculates area using Shoelace formula on equirectangular-projected coordinates. Displays area in m²/ft² (small) or km²/mi² (large) in a glassmorphism label at centroid. Label tracks map movement and dismisses on click. Full vertex dot and SVG overlay cleanup.
- [x] ~~**Geo-Pin Export CSV**~~ — ✅ DONE. `📥 Export Pins CSV` context menu item. `exportPinsCSV()` converts all loaded pins to CSV with columns: signature, sender, lat, lng, message, timestamp, date(ISO). Proper double-quote escaping for message field. Triggers browser download as `warmaps-pins-{timestamp}.csv`. Toast notification on success/empty.
- [x] ~~**Geo-Pin Import CSV**~~ — ✅ DONE. `📤 Import Pins CSV` context menu item. `importPinsCSV()` creates hidden file input (`.csv` only), uploads via FormData to `POST /api/geo-pins/import`. Server-side CSV parser handles quoted fields with escaped quotes. Validates required columns (sender, lat, lng, message), generates signatures for imports. Refreshes all map cluster and heatmap sources after import. Toast shows count.
- [x] ~~**Annotation Text Labels**~~ — ✅ DONE. `🔤 Text` toggle button in drawing toolbar (blue default, purple when active). In text mode, clicking the SVG overlay prompts for label text, places a geo-positioned SVG text element with current color and width-scaled font size (10 + width*2). Text annotations stored with `text` and `fontSize` properties. `renderAnnotationsOnMap` splits lines vs text — text annotations render as MapLibre symbol layers with halo for readability. Mutually exclusive with eraser mode.
- [x] ~~**Annotation Shapes**~~ — ✅ DONE. Added ⬜ Rect and ⭕ Circle toggle buttons to drawing toolbar. `shapeMode` state tracks active shape. Mousedown creates SVG preview (rect or ellipse) with current color + semi-transparent fill. Mousemove updates dimensions live. Mouseup converts to geo-coordinates — rectangles as 4-corner closed polygons, circles as 32-point polygon approximations for LineString compatibility. All mode buttons now mutually exclusive via `resetModes()` helper.
- [x] ~~**Geo-Pin Notification Sound**~~ — ✅ DONE. `playPinNotification()` uses Web Audio API to synthesize a two-tone chime (440Hz→660Hz, 80ms each, 0.15 volume with exponential decay). Fires when a remote geo-pin arrives via WebSocket. Throttled to max 1 sound per 2 seconds. Skipped when tab is hidden (`document.hidden`). No external audio files needed.
- [x] ~~**Geo-Pin Bookmark/Favorites**~~ — ✅ DONE. `☆ Bookmark` toggle button in each pin popup. LocalStorage-backed (`warmaps:pin-bookmarks`) Set of signatures. `isPinBookmarked()`, `togglePinBookmark()`, `getBookmarkedPins()` helpers. Button toggles between ☆/⭐ with yellow highlight. ⭐ filter button in filter bar filters to only bookmarked pins. Clear button resets bookmark filter.
- [x] ~~**Annotation Undo/Redo**~~ — ✅ DONE. Ctrl+Z undoes last annotation (pops from `drawnAnnotations`, removes last SVG child, pushes to `redoStack`). Ctrl+Y redoes (pops from `redoStack`, re-appends SVG element). Toast shows count. Keyboard handler cleaned up on drawing exit. Works with all annotation types: freehand, text, shapes.
- [x] ~~**Geo-Pin Filtering**~~ — ✅ DONE. Floating glassmorphism filter bar at bottom-left of map with 🔍 icon. Text search input filters pins by sender or message content (case-insensitive). Two date pickers (from/to) filter by timestamp range. Clear button resets all filters. `applyFilter()` filters `allLoadedPins` client-side and updates both cluster (`geo-pins-markers`) and heatmap (`geo-pins-heat`) sources on all map instances in real-time.
- [x] ~~**Annotation Color Presets**~~ — ✅ DONE. 8 tactical color dots (purple, red, green, blue, yellow, orange, cyan, white) in drawing toolbar between color picker and width slider. Click to set `currentColor` and sync native `<input type="color">`. Active dot gets white border ring with 150ms transition. Using native color picker deselects all presets.
- [x] ~~**Map Keyboard Shortcuts**~~ — ✅ DONE. Arrow keys pan 100px (Shift+Arrow pans 300px). +/= zoom in, -/_ zoom out. Number keys 1-5 flyTo preset locations (DC, London, Kyiv, Taipei, Jerusalem) with toast notification. Skipped when input/textarea focused or drawing mode active.
- [x] ~~**Geo-Pin Attachment Gallery**~~ — ✅ DONE. `🖼️ Media Gallery` context menu item. `showMediaGallery()` opens glassmorphism overlay (fixed, z-10000) with 3-column CSS grid. Fetches lightweight listing from `GET /api/geo-pins/gallery` (new `getAllPinMedia()` query — returns metadata without base64). Cards lazy-load thumbnails from `/api/geo-pins/media?signature=...`. Each card shows 120px object-fit-cover image, mimeType badge, and relative timestamp. Hover scales 1.03x with accent border. Click opens full-size in new tab. Close via ×, Escape, or backdrop click.
- [x] ~~**Geo-Pin Map Cluster Stats**~~ — ✅ DONE. Glassmorphism pill widget at top-left showing pin count (purple accent) + date range (oldest — newest). Auto-refreshes every 10s via `updateStats()`. Shows "No pins" state when empty. Injected via `addPinStatsWidget()` with WeakSet dedup.
- [x] ~~**Annotation Layer Toggle**~~ — ✅ DONE. 👁️ eye button at top-right, toggles all `ann-*` MapLibre layers between visible/none. Swaps emoji to 🚫 when hidden. Toast confirms state change. `addAnnotationToggle()` with WeakSet dedup.
- [x] ~~**Annotation Export/Import**~~ — ✅ DONE. `💾 Export Annotations` and `📂 Import Annotations` context menu items. Export fetches from `/api/annotations`, converts to GeoJSON FeatureCollection with properties (color, width, timestamp), triggers `.geojson` download. Import opens file picker for `.geojson/.json`, validates FeatureCollection, filters LineString features, POSTs to `/api/annotations`, renders on map. Error toasts for invalid files.
- [x] ~~**Geo-Pin Thread Replies UI**~~ — (deferred to next session)
- [x] ~~**Heatmap Intensity Control**~~ — ✅ DONE. 🔥 toggle button at bottom-right opens a glassmorphism panel with Radius and Intensity range sliders. Sliders directly update MapLibre `heatmap-radius` and `heatmap-intensity` paint properties in real-time via `setPaintProperty()`. Panel toggled on/off with click. Purple accent styling.
- [x] ~~**Multi-Map Pin Sync**~~ — ✅ DONE. When a pin cluster is clicked on one map, all other map instances (from `mapInstances` array) `flyTo()` the same coordinates and zoom level with 1200ms animation. Enables synchronized multi-view navigation.
- [x] ~~**Annotation Collaboration**~~ — ✅ DONE. Wired `onRemoteAnnotation()` listener in `initMapContextMenu()`. When a peer broadcasts a drawn annotation via WebSocket sync, it's rendered on all local map instances via `renderAnnotationsOnMap()`. Toast shows peer name. Completes the real-time collaboration loop (broadcast was already wired in drawing save).
- [x] ~~**Map Compare Mode**~~ — ✅ DONE. 🔗 Compare Mode context menu toggle. When active, all map instances sync pan/zoom/bearing/pitch via `moveend`/`zoomend` listeners with `compareSyncing` lock to prevent infinite loops. Uses `jumpTo()` for instant sync. Glassmorphism '🔗 Synced' badge on each map. Toggle off removes badges and stops syncing.
- [x] ~~**Custom Map Style Switcher**~~ — ✅ DONE. 🌐 Map Style context menu item. Cycles between Dark (CartoDB Dark Matter), Satellite (MapTiler Hybrid), and Terrain (CartoDB Voyager) styles. Uses `map.setStyle()` with automatic geo-pin layer re-initialization on `style.load` event. Per-map style index tracking.
- [x] ~~**Geo-Pin Cluster Heatmap Toggle**~~ — ✅ DONE. 🔄 Toggle Heatmap/Clusters context menu item. Switches between cluster markers (geo-pins-clusters, geo-pins-cluster-count, geo-pins-unclustered) and heatmap density (geo-pins-heatmap) layer visibility. Toast shows active mode.
- [x] ~~**Map Bookmarks**~~ — ✅ DONE. 🔖 Bookmark View saves current viewport (center, zoom, bearing, pitch) with custom name to `localStorage`. 📚 Load Bookmark opens glassmorphism overlay listing saved bookmarks with date + zoom level. Click to fly, 🗑 delete button with instant re-render. Close via ✕, Escape, or backdrop.
- [x] ~~**Pin Category System**~~ — ✅ DONE. 5 categories (Intel 🔍, Alert ⚠️, Logistics 📦, Social 💬, Other 📌) each with distinct color. `selectPinCategory()` shows glassmorphism popup during pin creation. Category stored in pin data and sent to API. Hover effects on category buttons.
- [x] ~~**Pin Search & Filter**~~ — ✅ DONE. 🔍 Floating search widget at top-center of each map. Text search filters pins by message (case-insensitive) with 300ms debounce. Category dropdown filters by intel/alert/logistics/social/other. Shows match count. Flies to first match. Escape clears filter and restores all pins. Prevents keyboard shortcuts while typing.
- [x] ~~**Map Offline Tiles**~~ — ✅ DONE. 📡 Cache Tiles Offline context menu item. Uses Cache API to download tiles for current viewport at ±1 zoom levels. Tile coordinate math (lng2tile/lat2tile) enumerates bounding box. Batched fetch (10 concurrent). 500-tile safety limit. Progress toast. Skips already-cached tiles.
- [x] ~~**Tactical Timeline**~~ — ✅ DONE. Glassmorphism timeline bar at bottom of each map. Range slider scrubs through pin history by timestamp. ▶/⏸ play/pause auto-advances at 200ms intervals. Date+time label shows current position. At 100% shows all pins. Filters `geo-pins-markers` source data in real-time.
- [x] ~~**Map Screenshot Annotations**~~ — ✅ DONE. 📷 Annotate Screenshot context menu item. Captures WebGL canvas, opens fullscreen editor overlay with freehand drawing (mouse drag), color cycling (6 colors), text labels (centered), and 💾 Save as PNG download. Close via ✕ or Escape.
- [x] ~~**Multi-User Cursor Trails**~~ — ✅ DONE. Broadcasts local mouse lng/lat over `__wmSyncChannel` WebSocket at 100ms throttle. Remote peers rendered as colored dots with name labels (auto-generated `peerId` + random HSL color). Cursors fade to opacity 0 after 5s inactivity. Positioned via `map.project()` for pixel-accurate placement.
- [x] ~~**Pin Voting System**~~ — ✅ DONE. `createVoteWidget(pinId)` returns HTML with 👍/👎 buttons and count display. Votes stored in `localStorage` under `warmaps-pin-votes`. Delegated click handler on document wires up/down votes globally. Scale animation on hover. `getVoteCount`/`votePin` helpers for external use.
- [x] ~~**Pin Export to GeoJSON**~~ — ✅ DONE. 💾 Export Pins GeoJSON context menu item. Converts all `allLoadedPins` to a valid GeoJSON FeatureCollection with Point geometry and properties (message, sender, signature, timestamp, category). Downloads as `.geojson` file via Blob/URL.
- [x] ~~**Map Distance Rings**~~ — ✅ DONE. ⭕ Distance Rings context menu item. Draws concentric circles at 1/5/10/25km around clicked point using `createCircleGeoJSON()` (64-point polygon approximation). Color-coded dashed line layers + text labels along the ring. Center dot marker. Auto-removes after 30 seconds.
- [x] ~~**Pin Import from GeoJSON**~~ — ✅ DONE. 📥 Import Pins GeoJSON context menu item. Opens file picker (`.geojson`/`.json`). Parses FeatureCollection, extracts Point features into `allLoadedPins` with message, sender, category, timestamp. Refreshes cluster sources on all map instances. Shows import count toast.
- [x] ~~**Map Grid Overlay**~~ — ✅ DONE. 🌐 Toggle Grid context menu item. Generates lat/lng graticule at 10° intervals (37 longitude lines, 17 latitude lines). GeoJSON line source with symbol labels. Toggle on/off with WeakMap state tracking. Purple-tinted lines matching WARMAPS theme.
- [x] ~~**Pin Attachments Gallery**~~ — ✅ DONE. 🖼️ Pin Gallery context menu item. Fullscreen glassmorphism overlay with auto-fill grid layout. Filters `allLoadedPins` for media attachments. Cards show thumbnail (150px), message, sender, and date. Hover scale + border glow animation. Close via ✕ or Escape.
- [x] ~~**Map Measure Area**~~ — ✅ DONE. 📐 Measure Area context menu item. Click-to-place polygon vertices with live visual feedback (fill + dashed outline + vertex dots). Double-click to finish and calculate area via Shoelace formula with spherical lat/lng→km conversion. Result in km². Auto-cleanup after 15 seconds.
- [x] ~~**Pin Thread Replies**~~ — ✅ DONE. 💬 `createThreadWidget(pinId)` returns HTML for threaded reply display + input. Replies stored in localStorage (`warmaps-pin-threads`) per pinId. Delegated click handler wires Send button with live DOM refresh. Shows reply count, sender, timestamp, and message. Scrollable reply container (120px max).
- [x] ~~**Map Bearing Indicator**~~ — ✅ DONE. 🧭 Bearing Line context menu item. Prompt for bearing angle (0-360°). Draws 50km dashed line using Vincenty-style spherical math. 10km tick marks with labels. Origin marker. Amber-colored layers. Auto-removes after 20 seconds.
- [x] ~~**Pin Heatmap Layer**~~ — ✅ DONE. 🔥 Pin Heatmap context menu toggle. Generates GeoJSON source from `allLoadedPins`. MapLibre heatmap layer with interpolated color ramp (blue→green→yellow→red). Zoom-adaptive radius (4’30px) and intensity (1–3). WeakMap toggle state. Shows pin count on activation.
- [x] ~~**Map Coordinate Copy**~~ — ✅ DONE. 📋 Copy Coordinates context menu item. Copies clicked lat/lng to clipboard in decimal format (6 decimal places). Toast shows both decimal and DMS (degrees/minutes/seconds) formats. `toDMS()` helper with N/S/E/W direction labels. Graceful fallback if clipboard API unavailable.
- [x] ~~**Pin Expiry Timer**~~ — ✅ DONE. ⏳ Pin Expiry Timer context menu toggle. Prompt for TTL in minutes. `PinExpiryConfig` persisted in localStorage. 10s interval checker removes expired pins by timestamp and refreshes clusters. Auto-starts on page load if configured. Toggle on/off.
- [x] ~~**Map Flyover Animation**~~ — ✅ DONE. ✈️ Flyover context menu item. Reads saved bookmarks from localStorage. Animates `map.flyTo()` between each stop with 3s easing + 1s pause. Progress toasts show stop name and index. Requires ≥2 bookmarks.
- [x] ~~**Pin Proximity Alert**~~ — ✅ DONE. 📍 Proximity Alert context menu toggle. Configurable radius (km) via prompt. Geolocation `watchPosition()` with high accuracy. Haversine distance check to all pins. Browser Notification API integration with permission request. Toggle on/off.
- [x] ~~**Map Weather Info**~~ — ✅ DONE. ⛅ Weather Info context menu item. Fetches from Open-Meteo API (free, no key). Shows temperature (°C), wind speed (km/h), and weather condition with emoji icons. `weatherCodeToText()` maps WMO codes to readable descriptions. Graceful error handling.
- [x] ~~**Pin Clustering Config**~~ — ✅ DONE. ⚙️ Cluster Settings context menu item. Adjustable cluster radius and max zoom via prompts. Updates cluster source `_options` on all map instances. Persists config in localStorage (`warmaps-cluster-config`). Shows current values as defaults.
- [x] ~~**Map Dark/Light Toggle**~~ — ✅ DONE. 🌓 Toggle Style context menu item. Switches between CartoDB Dark Matter and Positron styles. Saves/restores all custom sources and layers after style swap via `style.load` event. Persists preference in localStorage. Moon/sun emoji feedback.
- [x] ~~**Pin Export CSV**~~ — ✅ DONE. 📄 Export Pins CSV context menu item. Generates CSV with lat, lng, message, sender, timestamp columns. Proper quote escaping for messages. Blob download via `URL.createObjectURL()`. Auto-named file with timestamp. Pin count in toast.
- [x] ~~**Map Scale Bar**~~ — ✅ DONE. 📏 Scale Bar context menu toggle. 100px gradient bar with distance label (m/km). Calculates meters-per-pixel from latitude and zoom using Web Mercator formula. Updates on zoom/move events. Glassmorphism styling. WeakMap toggle state.
- [x] ~~**Pin Bulk Delete**~~ — ✅ DONE. 🗑️ Bulk Delete Pins context menu item. Prompts for count or "all". Confirmation dialog before deletion. Removes from `allLoadedPins` (most recent first). Refreshes clusters on all maps. Shows remaining count.
- [x] ~~**Map Night Vision**~~ — ✅ DONE. 🔭 Night Vision context menu toggle. Green-tinted radial gradient overlay with `mix-blend-mode: multiply`. Scanlines effect via repeating gradient. Vignette darkening at edges. CSS filter on canvas: desaturate → sepia → hue-rotate(70deg) for authentic NVG green.
- [x] ~~**Pin Statistics Dashboard**~~ — ✅ DONE. 📊 context menu item opens glassmorphism overlay with 4 summary cards (Total Pins, Unique Senders, Last 24h, Last 7d), top 8 senders with gradient bar chart, 7-day activity histogram, category breakdown pills with color-coded icons, and date range info with avg pins/day. Uses allLoadedPins data. Closes via ×, Escape, or backdrop click.
- [x] ~~**Map Terrain Contours**~~ — ✅ DONE. 🏔️ context menu toggle adds AWS Terrain Tiles (free, no API key) as raster-dem source with dual hillshade layers: primary layer has purple-tinted shadows (illumination 315°, viewport-anchored) and secondary layer adds accent banding (illumination 45°, map-anchored). Smart layer insertion below labels. Status widget in bottom-right. WeakMap toggle state. Clean removal of source + layers on toggle off.
- [x] ~~**Pin Statistics Export**~~ — ✅ DONE. Added a `📄 Export Report` button tightly integrated into the top bar of the Pin Statistics Dashboard that downloads a clean, properly escaped CSV output using Blob storage.
- [x] ~~**Pin Density Heatmap in Stats**~~ — ✅ DONE. Integrated a nested mini MapLibre geographic heatmap into the statistics dashboard (`div id="stats-mini-map"`). Fully initialized dynamically, taking all fetched pins from the API as a GeoJSON source. Uses native zoom autofit with padded bounds when pins change.

## 🔴 Priority: Fix
- [x] ~~**geeksy.xyz DNS not resolving**~~ — ✅ DONE. DNS A record (202.155.132.139) confirmed resolving via Google DNS. Caddy reverse proxy serving HTTPS correctly. Also migrated geeksy from systemd to bgrun for dashboard-based deploy/restart.

## 🟡 Priority: Improve
- [x] ~~**No systemd services for production processes**~~ — ✅ DONE. Created `warmaps.service`, `geeksy.service`, and `bgrun.service` in `/etc/systemd/system/` with `Restart=always` and `enabled` on boot. Previously all bun processes ran as bare nohup jobs with no watchdog — when they crashed after 102 days uptime, nothing restarted them.

## 📝 Architecture Notes
- **Production**: `root@202.155.132.139:/opt/starwar/`
- **Deploy**: `git push && ssh pull + bun run start` — **IMPORTANT**: always kill existing process before restart. PID file alone is not reliable.
- **HTTPS**: Caddy reverse proxy on port 443 → localhost:4444
- **TypeScript**: Zero errors (`npx tsc --noEmit` = clean)
- **Tests**: 59 passing (db integration + canvas modules + API endpoints). CI via GitHub Actions.
- **Map**: MapLibre GL with GeoJSON sources (fires, flights, events, assets, acled, webcams, seismic, crypto, telegram)
- **Canvas engine**: `app/lib/warmaps-canvas.ts` (328 lines) — GalaxyDraw adapter + thin orchestrator
- **Container drag**: `app/lib/container-drag.ts` (~145 lines) — Mouse + touch drag with DragContext interface
- **Canvas layout**: `app/lib/canvas-layout.ts` (~180 lines) — Minimap, fit-all, auto-arrange, bounds helpers
- **Keyboard shortcuts**: `app/lib/keyboard-shortcuts.ts` (~136 lines) — Extracted with dependency injection
- **Snap guidelines**: `app/lib/snap-guidelines.ts` (~120 lines) — SVG alignment guides for container dragging
- **Command palette**: `app/lib/command-palette.ts` (~165 lines) — Ctrl+K launcher with 19 conflict zones
- **Map context menu**: `app/lib/map-context-menu.ts` (~550 lines) — Right-click: Copy, Pin, Zoom, AI, Geo-Pin, Ruler
- **Geo-pins**: `app/lib/geo-pins.ts` — On-chain Solana memo + SQLite persistence + WS broadcast
- **Telegram OSINT**: `src/telegram.ts` — 22 channels, 95 location patterns, 8 equipment categories, auto-reconnect
- **Widget system**: `app/lib/widgets.ts` — 13 widget types, instance management, share link encoding
- **Client**: `app/page.client.tsx` → thin orchestrator importing 17 modules from `app/lib/`
- **API routes**: `/api/gdelt`, `/api/acled`, `/api/fires`, `/api/flights`, `/api/telegram/*`, `/api/mm`, `/api/bet`, `/api/image-proxy`, `/api/swap`, `/api/health`, `/api/manifest.json`, `/api/robots.txt`, `/api/sitemap.xml`, `/api/og-image`, `/api/ping`, `/api/geo-pins`, `/api/geocode`
- **WebSocket channels**: `/ws/chat` (live chat), `/ws/sync` (collaborative cursors), `/ws/geo-pins` (real-time pins)
- **Database**: `starwar_users.db` — users, sessions, user_preferences, geo_pins (sqlite-zod-orm)

## ⚠️ Security Reminders
- **NO wallet data on public site** — MM panel removed, `/api/mm` returns `running: false`
- **Commit messages**: Never mention wallets, MM, security fixes in public commit messages
- Git history was squashed to remove incriminating messages (force-pushed)
- `.config.toml` and `mm_state*.json` are gitignored
